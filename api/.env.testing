APP_NAME=LifeBuffer
APP_ENV=testing
APP_KEY=base64:bKQUl5rtlIa7hPadkJmZTksO3zvRvw4X8SwH32E+Xk4=
APP_DEBUG=true
APP_URL=http://api.lifebuffer.test

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

BCRYPT_ROUNDS=4

LOG_CHANNEL=single
LOG_LEVEL=debug

# Use SQLite for testing to avoid database dependencies
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Use array driver for sessions in testing
SESSION_DRIVER=array
SESSION_LIFETIME=120

# Use sync driver for broadcasting
BROADCAST_CONNECTION=sync

# Use local filesystem for testing
FILESYSTEM_DISK=local

# Use sync queue for testing
QUEUE_CONNECTION=sync

# Use array cache for testing
CACHE_STORE=array

# Disable Redis for testing
REDIS_CLIENT=array

# Use array mail driver for testing
MAIL_MAILER=array

# Disable Telescope for testing
TELESCOPE_ENABLED=false